import { promises as fs } from "node:fs";
import path from "node:path";

const HELIUS_API_KEY=process.env.HELIUS_API_KEY?.trim();
const VYBE_API_KEY=process.env.VYBE_API_KEY?.trim();
const STATE_PATH=process.env.SCOUT_STATE_PATH||"./data/shark-state.json";
const CHECKPOINT_PATH=process.env.SCOUT_GAUNTLET_STATE_PATH||"./data/gauntlet-state.json";
const REPORT_PATH=process.env.SCOUT_GAUNTLET_PATH||"./data/latest-gauntlet.json";
const CACHE_DIR=process.env.SCOUT_HELIUS_CACHE_DIR||"./data/helius-cache";
const PREFILTER_LIMIT=clamp(Number(process.env.GAUNTLET_PREFILTER_LIMIT||260),20,700);
const FULL_LIMIT=clamp(Number(process.env.GAUNTLET_FULL_LIMIT||160),10,350);
const TIME_BUDGET_MS=clamp(Number(process.env.GAUNTLET_TIME_BUDGET_SECONDS||840),120,3000)*1000;
const CONCURRENCY=clamp(Number(process.env.GAUNTLET_CONCURRENCY||2),1,6);
const HELIUS_PAGES=clamp(Number(process.env.GAUNTLET_HELIUS_PAGES||5),1,12);
const HELIUS_MIN_INTERVAL_MS=clamp(Number(process.env.GAUNTLET_HELIUS_MIN_INTERVAL_MS||300),100,3000);
const CACHE_MAX_AGE_MIN=clamp(Number(process.env.GAUNTLET_CACHE_MAX_AGE_MINUTES||180),10,1440);
const VYBE_LOOKBACK_HOURS=clamp(Number(process.env.GAUNTLET_VYBE_LOOKBACK_HOURS||23),6,47);
const UNKNOWN_RETRY_HOURS=clamp(Number(process.env.GAUNTLET_UNKNOWN_RETRY_HOURS||6),1,168);
const TIMEOUT_MS=clamp(Number(process.env.REQUEST_TIMEOUT_MS||25000),3000,60000);
const FOLLOW_SIZE=.075,ODIN_RATE=.01,TIP_RATE=.003,NETWORK_PER_LEG=.00015,EVALUATOR_VERSION="v6.0";
const WSOL="So11111111111111111111111111111111111111112",USDC="EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v",USDT="Es9vMFrzaCERmJfrF4H2FYD4KCoNkY11McCe8BenwNYB";
const QUOTES=new Set([WSOL,USDC,USDT]);

type Wallet={address:string;status?:string;tokens?:string[];providers?:string[];rediscoveryCount?:number;tags?:string[];profile?:any};
type Lot={t:number;qty:number|null;buySol:number|null};
type RT={mint:string;buyTimestamp:number;sellTimestamp:number;holdSeconds:number;buySol:number|null;sellSol:number|null;sourceRoi:number|null;followerNetSol:number|null;followerRoi:number|null;stress50NetSol:number|null;stress75NetSol:number|null};
type Result={address:string;fingerprint:string;evaluatedAt:string;discoveryTokens:number;rediscoveryCount:number;providers:string[];prefilter:any;heliusTransactions:number;heliusSwaps:number;heliusCoveragePartial:boolean;heliusError:string|null;vybeTradesWindow:number;vybeLookbackHours:number;vybeError:string|null;hold:any;replay:any;robustness:any;dataQuality:any;verdict:any};
type CP={schemaVersion:number;updatedAt:string;results:Record<string,Result>;runs:any[]};

function clamp(n:number,min:number,max:number){return Math.max(min,Math.min(max,Number.isFinite(n)?Math.floor(n):min));}
function median(v:number[]){if(!v.length)return null;const a=[...v].sort((x,y)=>x-y),m=Math.floor(a.length/2);return a.length%2?a[m]!:(a[m-1]!+a[m]!)/2;}
function quantile(v:number[],p:number){if(!v.length)return null;const a=[...v].sort((x,y)=>x-y);return a[Math.min(a.length-1,Math.floor((a.length-1)*p))]!;}
function score(w:Wallet){return(w.tokens?.length||0)*5000+Math.min(w.rediscoveryCount||0,1000)*10+Math.min(w.profile?.heliusRecent?.swapsFetched||0,50);}
function fingerprint(w:Wallet){return`${EVALUATOR_VERSION}|${(w.tokens||[]).slice().sort().join(",")}|${(w.tags||[]).slice().sort().join(",")}|${w.status||""}`;}
function badTag(tags:string[]=[]){const s=tags.join(" ").toLowerCase();for(const x of["sniper","bundler","insider","developer","bot","mev","exchange","cex"])if(s.includes(x))return x;return null;}
function sleep(ms:number){return new Promise(r=>setTimeout(r,ms));}
function deadline(start:number){return Date.now()-start>=TIME_BUDGET_MS;}
function retryable(s:number){return s===408||s===425||s===429||s>=500;}
const providerStats={heliusCalls:0,vybeCalls:0,retries:0,rateLimits:0,errors:0,cacheHits:0,cacheMisses:0};
async function fetchJson(url:string,init:RequestInit={},attempts=3){let last:Error|undefined;for(let a=0;a<attempts;a++){const c=new AbortController(),t=setTimeout(()=>c.abort(),TIMEOUT_MS);try{const r=await fetch(url,{...init,signal:c.signal});const text=await r.text();if(r.ok)return text?JSON.parse(text):null;if(r.status===429)providerStats.rateLimits++;last=new Error(`${r.status}:${text.slice(0,180)}`);if(!retryable(r.status)||a===attempts-1)throw last;providerStats.retries++;const ra=Number(r.headers.get("retry-after"));await sleep(Number.isFinite(ra)&&ra>0?ra*1000:650*(2**a)+Math.floor(Math.random()*250));}catch(e){last=e instanceof Error?e:new Error(String(e));if(a===attempts-1){providerStats.errors++;throw last;}providerStats.retries++;await sleep(650*(2**a)+Math.floor(Math.random()*250));}finally{clearTimeout(t);}}throw last||new Error("fetch failed");}
async function atomicSave(file:string,data:any){await fs.mkdir(path.dirname(file),{recursive:true});const tmp=`${file}.${process.pid}.tmp`;await fs.writeFile(tmp,JSON.stringify(data));await fs.rename(tmp,file);}
async function loadCP():Promise<CP>{try{const x=JSON.parse(await fs.readFile(CHECKPOINT_PATH,"utf8"));return{schemaVersion:7,updatedAt:x.updatedAt||new Date().toISOString(),results:x.results||{},runs:Array.isArray(x.runs)?x.runs:[]};}catch{return{schemaVersion:7,updatedAt:new Date().toISOString(),results:{},runs:[]};}}
async function loadWalletCache(address:string){try{const p=path.join(CACHE_DIR,`${address}.json`),st=await fs.stat(p);if(Date.now()-st.mtimeMs>CACHE_MAX_AGE_MIN*60000)return null;const x=JSON.parse(await fs.readFile(p,"utf8"));if(!Array.isArray(x?.rows))return null;providerStats.cacheHits++;return x.rows;}catch{providerStats.cacheMisses++;return null;}}
let heliusNextAt=0;async function heliusThrottle(){const w=Math.max(0,heliusNextAt-Date.now());if(w)await sleep(w);heliusNextAt=Math.max(Date.now(),heliusNextAt)+HELIUS_MIN_INTERVAL_MS;}
async function heliusPage(address:string,before:string|undefined,swapOnly:boolean,limit=100){await heliusThrottle();providerStats.heliusCalls++;const u=new URL(`https://api.helius.xyz/v0/addresses/${address}/transactions`);u.searchParams.set("api-key",HELIUS_API_KEY!);u.searchParams.set("limit",String(limit));if(before)u.searchParams.set("before",before);if(swapOnly)u.searchParams.set("type","SWAP");return fetchJson(u.toString(),{},4);}
async function recentRows(address:string){const c=await loadWalletCache(address);if(c)return{rows:c,error:null,cache:true};if(!HELIUS_API_KEY)return{rows:[] as any[],error:"HELIUS_API_KEY missing",cache:false};try{const x=await heliusPage(address,undefined,true,40);return{rows:Array.isArray(x)?x:[],error:null,cache:false};}catch(e){return{rows:[] as any[],error:String(e),cache:false};}}
async function heliusHistory(address:string,seed:any[]=[]){if(!HELIUS_API_KEY)return{rows:seed,error:"HELIUS_API_KEY missing",partial:true};const all=[...seed],seen=new Set(seed.map(x=>x?.signature).filter(Boolean));let before=seed.length?seed[seed.length-1]?.signature:undefined,partial=false;try{for(let page=0;page<HELIUS_PAGES;page++){let x:any;try{x=await heliusPage(address,before,true,100);}catch(e){if(page===0&&String(e).includes("400"))x=await heliusPage(address,before,false,100);else throw e;}if(!Array.isArray(x)||!x.length)break;let added=0;for(const row of x){if(!row?.signature||!seen.has(row.signature)){all.push(row);if(row?.signature)seen.add(row.signature);added++;}}before=x[x.length-1]?.signature;if(x.length<100||added===0)break;if(page===HELIUS_PAGES-1&&x.length===100)partial=true;}all.sort((a,b)=>Number(b?.timestamp||0)-Number(a?.timestamp||0));return{rows:all,error:null,partial};}catch(e){return{rows:all,error:String(e),partial:true};}}
async function vybeTrades(address:string){if(!VYBE_API_KEY)return{rows:[] as any[],error:"VYBE_API_KEY missing"};try{providerStats.vybeCalls++;const end=Math.floor(Date.now()/1000)-300,start=end-VYBE_LOOKBACK_HOURS*3600,q=new URLSearchParams({authorityAddress:address,timeStart:String(start),timeEnd:String(end),limit:"500",page:"0",sortByAsc:"blockTime"}),x=await fetchJson(`https://api.vybenetwork.xyz/v4/trades?${q}`,{headers:{"X-API-Key":VYBE_API_KEY}},3);return{rows:Array.isArray(x?.data)?x.data:[],error:null};}catch(e){return{rows:[],error:String(e)};}}
function num(v:any){const n=Number(v);return Number.isFinite(n)?n:null;}
function nativeSol(x:any){const n=num(x?.amount);return n==null?null:n/1e9;}
function econ(sourceRoi:number|null,retention=1){if(sourceRoi==null)return{net:null,roi:null};const bf=Math.max(FOLLOW_SIZE*ODIN_RATE,.001),bt=FOLLOW_SIZE*TIP_RATE,cost=FOLLOW_SIZE+bf+bt+NETWORK_PER_LEG,gross=Math.max(0,FOLLOW_SIZE*(1+sourceRoi*retention)),sf=Math.max(gross*ODIN_RATE,.001),st=gross*TIP_RATE,net=gross-sf-st-NETWORK_PER_LEG-cost;return{net,roi:net/cost};}
function quoteSol(tx:any,wallet:string,dir:"out"|"in"){let total=0,seen=false;for(const x of tx?.nativeTransfers||[]){const hit=dir==="out"?x?.fromUserAccount===wallet:x?.toUserAccount===wallet;if(hit){const v=nativeSol(x);if(v!=null){total+=v;seen=true;}}}for(const x of tx?.tokenTransfers||[]){if(x?.mint!==WSOL)continue;const hit=dir==="out"?x?.fromUserAccount===wallet:x?.toUserAccount===wallet;if(hit){const v=num(x?.tokenAmount);if(v!=null){total+=v;seen=true;}}}return seen?total:null;}
function extract(tx:any,wallet:string){const buys:Array<{mint:string;qty:number|null}>=[],sells:Array<{mint:string;qty:number|null}>=[],ev=tx?.events?.swap;if(ev){for(const x of ev.tokenOutputs||[])if(x?.userAccount===wallet&&!QUOTES.has(x?.mint))buys.push({mint:x.mint,qty:num(x?.rawTokenAmount?.tokenAmount??x?.tokenAmount)});for(const x of ev.tokenInputs||[])if(x?.userAccount===wallet&&!QUOTES.has(x?.mint))sells.push({mint:x.mint,qty:num(x?.rawTokenAmount?.tokenAmount??x?.tokenAmount)});}if(!buys.length&&!sells.length){for(const x of tx?.tokenTransfers||[]){if(!x?.mint||QUOTES.has(x.mint))continue;const q=num(x?.tokenAmount);if(x?.toUserAccount===wallet&&x?.fromUserAccount!==wallet)buys.push({mint:x.mint,qty:q});if(x?.fromUserAccount===wallet&&x?.toUserAccount!==wallet)sells.push({mint:x.mint,qty:q});}}return{buys,sells,buyQuoteSol:nativeSol(ev?.nativeInput)??quoteSol(tx,wallet,"out"),sellQuoteSol:nativeSol(ev?.nativeOutput)??quoteSol(tx,wallet,"in")};}
function holds(rows:any[],wallet:string){const ordered=[...rows].filter(x=>Number.isFinite(Number(x?.timestamp))).sort((a,b)=>Number(a.timestamp)-Number(b.timestamp)),open=new Map<string,Lot[]>(),rts:RT[]=[];let buys=0,sells=0,parsed=0;for(const tx of ordered){const t=Number(tx.timestamp),ex=extract(tx,wallet);if(!ex.buys.length&&!ex.sells.length)continue;parsed++;for(const b of ex.buys){buys++;const a=open.get(b.mint)||[];a.push({t,qty:b.qty,buySol:ex.buys.length===1?ex.buyQuoteSol:null});open.set(b.mint,a);}for(const s of ex.sells){sells++;const a=open.get(s.mint)||[];if(!a.length)continue;let remaining=s.qty;while(a.length&&(remaining==null||remaining>0)){const lot=a[0]!,lq=lot.qty,cq=remaining==null||lq==null?lq:Math.min(remaining,lq),frac=lq&&cq!=null?Math.min(1,cq/lq):1,buySol=lot.buySol==null?null:lot.buySol*frac,sellSol=ex.sells.length===1&&ex.sellQuoteSol!=null?ex.sellQuoteSol*frac:null,src=buySol&&sellSol!=null?(sellSol-buySol)/buySol:null,a1=econ(src,1),a5=econ(src,.5),a25=econ(src,.25);rts.push({mint:s.mint,buyTimestamp:lot.t,sellTimestamp:t,holdSeconds:Math.max(0,t-lot.t),buySol,sellSol,sourceRoi:src,followerNetSol:a1.net,followerRoi:a1.roi,stress50NetSol:a5.net,stress75NetSol:a25.net});if(remaining==null||lq==null||cq==null||cq>=lq){a.shift();remaining=null;}else{lot.qty=lq-cq;if(lot.buySol!=null)lot.buySol*=1-frac;remaining-=cq;}}open.set(s.mint,a);}}const hs=rts.map(x=>x.holdSeconds),fr=rts.map(x=>x.followerRoi).filter((x):x is number=>x!=null),nets=rts.map(x=>x.followerNetSol).filter((x):x is number=>x!=null),n50=rts.map(x=>x.stress50NetSol).filter((x):x is number=>x!=null),n75=rts.map(x=>x.stress75NetSol).filter((x):x is number=>x!=null);return{closedHolds:hs.length,medianHoldSeconds:median(hs),p25HoldSeconds:quantile(hs,.25),p75HoldSeconds:quantile(hs,.75),fastUnder10m:hs.filter(x=>x<600).length,slowOver6h:hs.filter(x=>x>=21600).length,idealOver12h:hs.filter(x=>x>=43200).length,buyEvents:buys,sellEvents:sells,parsedSwaps:parsed,estimatedFollowerTrades:fr.length,estimatedFollowerWinRate:fr.length?fr.filter(x=>x>0).length/fr.length:null,estimatedFollowerMedianRoi:median(fr),estimatedFollowerNetSol:nets.length?nets.reduce((a,b)=>a+b,0):null,stress50NetSol:n50.length?n50.reduce((a,b)=>a+b,0):null,stress75NetSol:n75.length?n75.reduce((a,b)=>a+b,0):null,roundTrips:rts.slice(-120)};}
function capped(rts:RT[],dailyCap:number){const groups=new Map<string,RT[]>();for(const r of rts){const k=`${r.mint}|${r.buyTimestamp}`,a=groups.get(k)||[];a.push(r);groups.set(k,a);}const entries=[...groups.values()].sort((a,b)=>a[0]!.buyTimestamp-b[0]!.buyTimestamp),daily=new Map<string,number>(),td=new Set<string>(),tw=new Set<string>();let selected=0,net=0,n50=0,n75=0,wins=0;for(const g of entries){const f=g[0]!,d=new Date(f.buyTimestamp*1000),day=d.toISOString().slice(0,10),week=`${d.getUTCFullYear()}-${Math.floor((Date.UTC(d.getUTCFullYear(),d.getUTCMonth(),d.getUTCDate())-Date.UTC(d.getUTCFullYear(),0,1))/604800000)}`,kd=`${f.mint}|${day}`,kw=`${f.mint}|${week}`;if((daily.get(day)||0)>=dailyCap||td.has(kd)||tw.has(kw))continue;const vals=g.map(x=>x.followerNetSol).filter((x):x is number=>x!=null);if(!vals.length)continue;const x=vals.reduce((a,b)=>a+b,0);net+=x;n50+=g.map(x=>x.stress50NetSol).filter((x):x is number=>x!=null).reduce((a,b)=>a+b,0);n75+=g.map(x=>x.stress75NetSol).filter((x):x is number=>x!=null).reduce((a,b)=>a+b,0);if(x>0)wins++;selected++;daily.set(day,(daily.get(day)||0)+1);td.add(kd);tw.add(kw);}return{dailyCap,selected,netSol:net,stress50NetSol:n50,stress75NetSol:n75,winRate:selected?wins/selected:null};}
function replay(h:any){return{onePerDay:capped(h.roundTrips||[],1),twoPerDay:capped(h.roundTrips||[],2)};}
function robustness(h:any,rp:any){const nets=(h.roundTrips||[]).map((x:any)=>x.followerNetSol).filter((x:any)=>typeof x==="number") as number[],pos=nets.filter(x=>x>0),tot=pos.reduce((a,b)=>a+b,0),largest=pos.length?Math.max(...pos):0;return{positiveTrades:pos.length,largestWinnerShare:tot>0?largest/tot:null,antiJackpotPass:tot>0?largest/tot<=.6:null,stress50Positive:typeof h.stress50NetSol==="number"?h.stress50NetSol>0:null,capped2Stress50Positive:rp.twoPerDay.selected>=3?rp.twoPerDay.stress50NetSol>0:null};}
function prefilterVerdict(w:Wallet,h:any,error:string|null){const tag=badTag(w.tags);if(tag)return{status:"REJECT",reason:`bad_tag:${tag}`,priority:-999};const med=h.medianHoldSeconds;if(h.closedHolds>=5&&med!=null&&med<600)return{status:"REJECT",reason:"recent_median_under_10m",priority:-500};if(h.closedHolds>=5&&med!=null&&med<3600)return{status:"SIGNAL_ONLY",reason:"recent_median_under_1h",priority:-100};const priority=score(w)+(med!=null?Math.min(med/60,1440):0)+(h.closedHolds>=3?2000:0)+(h.slowOver6h||0)*3000+(h.idealOver12h||0)*5000-(error?2000:0);return{status:"PASS",reason:h.closedHolds?"recent_behavior_viable":"insufficient_recent_closes",priority};}
function dataQuality(h:any,hx:any,vt:any){const hs=Math.min(1,h.closedHolds/10),ts=Math.min(1,h.estimatedFollowerTrades/8),cp=hx.partial?.15:0,ep=hx.error?.25:0;return{score:Math.max(0,Math.min(1,.55*hs+.35*ts+.10*(vt.rows.length>0?1:0)-cp-ep)),closedHoldAdequate:h.closedHolds>=5,economicSampleAdequate:h.estimatedFollowerTrades>=5,vybeAvailable:vt.rows.length>0,heliusPartial:hx.partial};}
function finalVerdict(w:Wallet,h:any,rp:any,r:any,q:any,errors:string[],partial:boolean){const tag=badTag(w.tags);if(tag)return{status:"REJECT",stage:"hard_gate",reasons:[`bad_tag:${tag}`]};const med=h.medianHoldSeconds,reasons:string[]=[];if(partial)reasons.push("helius_history_window_partial");if(h.closedHolds<5)reasons.push("closed_hold_sample_under_5");if(med==null)reasons.push("hold_time_unknown");else if(med<600)reasons.push("median_hold_under_10m");else if(med<3600)reasons.push("median_hold_10m_to_1h");else if(med<21600)reasons.push("median_hold_1h_to_6h");else if(med<43200)reasons.push("median_hold_6h_plus");else reasons.push("median_hold_12h_plus");if((w.tokens?.length||0)<2)reasons.push("single_discovery_token");else reasons.push(`cross_token_${w.tokens!.length}`);if(errors.length)reasons.push(...errors);if(h.closedHolds>=3&&med!=null&&med<600)return{status:"REJECT",stage:"hold_time",reasons};if(h.closedHolds>=3&&med!=null&&med<3600)return{status:"SIGNAL_ONLY",stage:"hold_time",reasons};if(h.estimatedFollowerTrades>=5&&h.estimatedFollowerNetSol!=null&&h.estimatedFollowerNetSol<=0)return{status:"REJECT",stage:"fixed_size_economics",reasons:[...reasons,"0.075_estimated_net_nonpositive"]};if(r.antiJackpotPass===false&&h.estimatedFollowerTrades>=5)return{status:"REJECT",stage:"robustness",reasons:[...reasons,"largest_winner_over_60pct_positive_pnl"]};if(med!=null&&med>=21600&&h.closedHolds>=8&&(w.tokens?.length||0)>=2&&rp.twoPerDay.selected>=3&&rp.twoPerDay.netSol>0&&rp.twoPerDay.stress50NetSol>0&&r.antiJackpotPass===true&&q.score>=.55)return{status:"DEEP_DIVE",stage:"odin_capped_replay",reasons:[...reasons,"0.075_two_per_day_replay_positive","anti_jackpot_pass","50pct_alpha_retention_positive","needs_exact_price_lag_liquidity_replay"]};if(med!=null&&med>=3600&&h.closedHolds>=5)return{status:"SIGNAL_ONLY",stage:"needs_more_economic_evidence",reasons};return{status:"UNKNOWN",stage:"insufficient_evidence",reasons};}
function stripHold(h:any){const{roundTrips,...rest}=h;return rest;}
function shouldEval(w:Wallet,prev:Result|undefined){if(!prev)return true;if(prev.fingerprint!==fingerprint(w))return true;if(prev.verdict?.status!=="UNKNOWN")return false;const transient=prev.verdict?.stage==="evaluation_error"||Boolean(prev.heliusError)||Boolean(prev.vybeError);if(!transient)return false;const age=Date.now()-Date.parse(prev.evaluatedAt||"");return !Number.isFinite(age)||age>=UNKNOWN_RETRY_HOURS*3600000;}
function lightweight(w:Wallet,pre:any,status:string):Result{return{address:w.address,fingerprint:fingerprint(w),evaluatedAt:new Date().toISOString(),discoveryTokens:w.tokens?.length||0,rediscoveryCount:w.rediscoveryCount||0,providers:w.providers||[],prefilter:pre,heliusTransactions:pre.recentRows||0,heliusSwaps:pre.recentRows||0,heliusCoveragePartial:true,heliusError:pre.error||null,vybeTradesWindow:0,vybeLookbackHours:VYBE_LOOKBACK_HOURS,vybeError:null,hold:{closedHolds:pre.closedHolds,medianHoldSeconds:pre.medianHoldSeconds},replay:{onePerDay:{selected:0,netSol:0},twoPerDay:{selected:0,netSol:0}},robustness:{},dataQuality:{score:0},verdict:{status,stage:"recent_prefilter",reasons:[pre.reason]}};}
async function evaluate(w:Wallet,seed:any[],pre:any):Promise<Result>{const[hx,vt]=await Promise.all([heliusHistory(w.address,seed),vybeTrades(w.address)]),h=holds(hx.rows,w.address),rp=replay(h),r=robustness(h,rp),q=dataQuality(h,hx,vt),errors:string[]=[];if(hx.error)errors.push("helius_history_error");if(vt.error)errors.push("vybe_history_unavailable");const v=finalVerdict(w,h,rp,r,q,errors,hx.partial);return{address:w.address,fingerprint:fingerprint(w),evaluatedAt:new Date().toISOString(),discoveryTokens:w.tokens?.length||0,rediscoveryCount:w.rediscoveryCount||0,providers:w.providers||[],prefilter:pre,heliusTransactions:hx.rows.length,heliusSwaps:hx.rows.filter(x=>x?.type==="SWAP").length,heliusCoveragePartial:hx.partial,heliusError:hx.error,vybeTradesWindow:vt.rows.length,vybeLookbackHours:VYBE_LOOKBACK_HOURS,vybeError:vt.error,hold:stripHold(h),replay:rp,robustness:r,dataQuality:q,verdict:v};}
function failed(w:Wallet,e:unknown,pre:any):Result{const msg=e instanceof Error?e.message:String(e);return{address:w.address,fingerprint:fingerprint(w),evaluatedAt:new Date().toISOString(),discoveryTokens:w.tokens?.length||0,rediscoveryCount:w.rediscoveryCount||0,providers:w.providers||[],prefilter:pre,heliusTransactions:0,heliusSwaps:0,heliusCoveragePartial:true,heliusError:msg,vybeTradesWindow:0,vybeLookbackHours:VYBE_LOOKBACK_HOURS,vybeError:null,hold:{closedHolds:0,medianHoldSeconds:null},replay:{onePerDay:{selected:0,netSol:0},twoPerDay:{selected:0,netSol:0}},robustness:{},dataQuality:{score:0},verdict:{status:"UNKNOWN",stage:"evaluation_error",reasons:["wallet_evaluation_exception"]}};}

export async function runGauntlet(){const wallStart=Date.now(),startedAt=new Date().toISOString(),state=JSON.parse(await fs.readFile(STATE_PATH,"utf8")),cp=await loadCP(),eligible=(Object.values(state.wallets||{}) as Wallet[]).filter(w=>w.status!=="REJECTED").sort((a,b)=>score(b)-score(a)),pending=eligible.filter(w=>shouldEval(w,cp.results[w.address])),preBatch=pending.slice(0,PREFILTER_LIMIT),passed:Array<{w:Wallet;rows:any[];pre:any}>=[];let prefiltered=0,preRejected=0,preSignal=0,lightweightClassified=0;for(const w of preBatch){if(deadline(wallStart))break;const rr=await recentRows(w.address),h=holds(rr.rows,w.address),pv=prefilterVerdict(w,h,rr.error),pre={evaluatedAt:new Date().toISOString(),status:pv.status,reason:pv.reason,priority:pv.priority,closedHolds:h.closedHolds,medianHoldSeconds:h.medianHoldSeconds,recentRows:rr.rows.length,cacheHit:rr.cache,error:rr.error};prefiltered++;if(pv.status==="REJECT"){preRejected++;cp.results[w.address]=lightweight(w,pre,"REJECT");lightweightClassified++;continue;}if(pv.status==="SIGNAL_ONLY"){preSignal++;cp.results[w.address]=lightweight(w,pre,"SIGNAL_ONLY");lightweightClassified++;continue;}passed.push({w,rows:rr.rows,pre});}passed.sort((a,b)=>(b.pre.priority||0)-(a.pre.priority||0));const full=passed.slice(0,FULL_LIMIT);let completed=0;for(let i=0;i<full.length&&!deadline(wallStart);i+=CONCURRENCY){const wave=full.slice(i,i+CONCURRENCY),results=await Promise.all(wave.map(async x=>{try{return await evaluate(x.w,x.rows,x.pre);}catch(e){return failed(x.w,e,x.pre);}}));for(const r of results)cp.results[r.address]=r;completed+=results.length;cp.updatedAt=new Date().toISOString();await atomicSave(CHECKPOINT_PATH,cp);}const current=eligible.map(w=>cp.results[w.address]).filter((x):x is Result=>Boolean(x)&&x.fingerprint.startsWith(`${EVALUATOR_VERSION}|`)),counts=current.reduce((a:any,x:any)=>(a[x.verdict.status]=(a[x.verdict.status]||0)+1,a),{}),deep=current.filter(x=>x.verdict.status==="DEEP_DIVE").sort((a,b)=>(b.replay?.twoPerDay?.stress50NetSol||-999)-(a.replay?.twoPerDay?.stress50NetSol||-999)),remaining=eligible.filter(w=>shouldEval(w,cp.results[w.address])).length,elapsedMs=Date.now()-wallStart,runSummary={startedAt,finishedAt:new Date().toISOString(),evaluatorVersion:EVALUATOR_VERSION,eligible:eligible.length,pendingAtStart:pending.length,prefiltered,preRejected,preSignal,prePassed:passed.length,lightweightClassified,fullEvaluated:completed,processedCumulative:current.length,remaining,counts,elapsedMs,timeBudgetMs:TIME_BUDGET_MS,providerStats,memoryMode:"bounded_per_wallet_cache_compact_checkpoint"};cp.runs=[...cp.runs.slice(-79),runSummary];cp.updatedAt=new Date().toISOString();await atomicSave(CHECKPOINT_PATH,cp);const report={schemaVersion:7,generatedAt:new Date().toISOString(),...runSummary,note:"v6 memory-bounded staged gauntlet: per-wallet compact cache -> decisive recent prefilter -> prioritized full reconstruction -> 0.075 SOL Odin capped replay. Full raw histories never persist in checkpoint/report.",deepDive:deep.slice(0,25)};await atomicSave(REPORT_PATH,report);console.log(JSON.stringify({event:"shark_scout_gauntlet_complete",...runSummary,deepDive:deep.slice(0,10).map(x=>({address:x.address,medianHoldSeconds:x.hold.medianHoldSeconds,closedHolds:x.hold.closedHolds,discoveryTokens:x.discoveryTokens,replay2DayNetSol:x.replay.twoPerDay.netSol,replay2DayStress50NetSol:x.replay.twoPerDay.stress50NetSol,replay2DayTrades:x.replay.twoPerDay.selected,largestWinnerShare:x.robustness.largestWinnerShare,dataQuality:x.dataQuality.score,reasons:x.verdict.reasons}))}));return report;}
if(import.meta.url===`file://${process.argv[1]}`)runGauntlet().catch(e=>{console.error(JSON.stringify({event:"shark_scout_gauntlet_failed",error:e instanceof Error?e.message:String(e)}));process.exitCode=1;});
